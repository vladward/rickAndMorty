import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  wrapper: {},
  container: {
    height: '100%',
    minHeight: '100vh',
    maxWidth: 'auto',
    background: 'rgb(36, 40, 47)',
    margin: '0 auto',
  },
});
