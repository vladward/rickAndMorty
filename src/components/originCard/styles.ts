import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  characterCardInfoOrigin: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    width: 'max-content',
    height: 'max-content',
    padding: '0 20px 20px 20px',
    margin: '20px',
    borderRadius: '20px !important',
  },
});
