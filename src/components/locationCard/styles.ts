import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  characterCardInfoLocation: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    width: 'max-content',
    height: 'max-content',
    margin: '20px',
    padding: '20px',
    borderRadius: '20px !important',
  },
});
