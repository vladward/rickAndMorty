import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  characterCardInfoEpisodes: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    justifyContent: 'space-between',
    margin: '20px',
    padding: '20px',
    borderRadius: '20px !important',
  },
});
