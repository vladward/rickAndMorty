import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  title: {
    color: 'white',
    textAlign: 'center',
    padding: '30px 0',
    fontFamily: 'RickAndMorty!important',
  },
});
