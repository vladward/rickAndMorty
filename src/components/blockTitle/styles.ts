import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  title: {
    color: 'white',
    textAlign: 'center',
    padding: '30px 0',
    maxWidth: '1660px',
    width: 'max-content',
    margin: '0 auto !important',
  },
});
